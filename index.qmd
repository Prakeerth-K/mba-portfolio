---
title: "Home"
---

<div class="reveal">
<h1>Hi, I’m Prakeerth 👋</h1>
<p>UCW MBA candidate focused on <strong>marketing strategy</strong>, <strong>cloud & analytics</strong>, and <strong>project execution</strong>.</p>
<p>
  <a class="badge" href="projects.qmd">See Projects</a>
  <a class="badge" href="resume.qmd">Resume</a>
  <a class="badge" href="skills.qmd">Skills</a>
  <a class="badge" href="contact.qmd">Contact</a>
</p>
</div>

<div class="reveal card">
  <h3 class="section-title">Highlights</h3>
  <ul>
    <li>Fresh Slice Pizza marketing campaign (STP, positioning, social activation)</li>
    <li>AWS Academy portfolio (IAM, VPC, S3, EC2, Data Governance)</li>
    <li>Predictive Analytics (DT, KNN, regression) — practical model comparisons</li>
  </ul>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const els = [...document.querySelectorAll('.reveal')];
  const io = new IntersectionObserver(entries => {
    entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('is-visible'); });
  }, { threshold: 0.15 });
  els.forEach(el => io.observe(el));
});
</script>
<!-- Go to Top Button -->
<button onclick="topFunction()" id="toTopBtn" title="Go to top">↑</button>

<script>
  // Show button when scrolling down
  const toTopBtn = document.getElementById("toTopBtn");
  window.onscroll = function() {
    if (document.body.scrollTop > 200 || document.documentElement.scrollTop > 200) {
      toTopBtn.style.display = "block";
    } else {
      toTopBtn.style.display = "none";
    }
  };

  // Scroll back to top
  function topFunction() {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }
</script>
